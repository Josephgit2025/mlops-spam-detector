apiVersion: v1
kind: Service
metadata:
  name: spam-detector
spec:
  selector:
    app: spam-detector
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
      nodePort: 30000
  type: LoadBalancer
